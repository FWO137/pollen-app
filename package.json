{
  "name": "pollen-app",
  "private": true,
  "description": "Pollenflug-Vorhersage für Süddeutschland & Österreich — static site, no build step.",
  "scripts": {
    "test": "node --test",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
